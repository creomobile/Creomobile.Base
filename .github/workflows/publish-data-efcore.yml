name: Publish Creomobile.Data.EFCore

on:
  push:
    tags:
      - "creomobile.data.efcore/v*"

jobs:
  publish:
    uses: ./.github/workflows/nuget-publish-reusable.yml
    with:
      package_id: Creomobile.Data.EFCore
      project_path: src/Data/Creomobile.Data.EFCore/Creomobile.Data.EFCore.csproj
      tag_prefix: creomobile.data.efcore/
    secrets: inherit
